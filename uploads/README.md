# uploads directory — stores original resume files during local development
# Files are saved as: <uuid>_<original-filename>
#
# This directory is intentionally NOT committed to git (see .gitignore).
# In production, replace with cloud storage (S3, GCS, etc.)
